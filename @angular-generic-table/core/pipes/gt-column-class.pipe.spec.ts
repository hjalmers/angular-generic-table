import { GtColumnClassPipe } from './gt-column-class.pipe';

describe('GtColumnClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GtColumnClassPipe();
    expect(pipe).toBeTruthy();
  });
});
