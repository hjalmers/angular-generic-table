import { GtTotalsPositionPipe } from './gt-totals-position.pipe';

describe('GtTotalsPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new GtTotalsPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
