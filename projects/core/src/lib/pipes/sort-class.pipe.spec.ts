import { SortClassPipe } from './sort-class.pipe';

describe('SortClassPipe', () => {
  it('create an instance', () => {
    const pipe = new SortClassPipe();
    expect(pipe).toBeTruthy();
  });
});
