import { RowSelectionPipe } from './row-selection.pipe';

describe('RowSelectionPipe', () => {
  it('create an instance', () => {
    const pipe = new RowSelectionPipe();
    expect(pipe).toBeTruthy();
  });
});
